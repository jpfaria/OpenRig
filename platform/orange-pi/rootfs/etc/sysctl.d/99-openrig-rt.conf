# Allow JACK to use real-time scheduling without kernel throttling.
# Default (950000) limits RT threads to 95% of a period, which causes
# sched_setscheduler(SCHED_RR) to fail with EPERM even for root.
kernel.sched_rt_runtime_us = -1
