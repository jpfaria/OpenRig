# Eagerly load the Mali-G610 kernel driver at boot. The DT alias in panthor.ko
# matches the RK3588 GPU node so udev would eventually bind it, but listing it
# here guarantees it comes up before weston tries to open /dev/dri/card1 and
# before Mesa clients initialize EGL.
panthor
