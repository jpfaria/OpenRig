// AUTO-GENERATED by tools/lv2_catalog_import — do not edit by hand.
// Re-run the tool to regenerate. Manual customisation goes in overrides.yaml.

#![allow(dead_code, unused_imports, unused_variables, clippy::approx_constant, clippy::excessive_precision)]

use crate::registry::ModModelDefinition;
use crate::ModBackendKind;
use anyhow::Result;
use block_core::param::{
    float_parameter, required_f32, ModelParameterSchema, ParameterSet, ParameterUnit,
};
use block_core::{AudioChannelLayout, BlockProcessor, ModelAudioMode};

pub const MODEL_ID: &str = "lv2_calf_bad_multi_chorus";
pub const DISPLAY_NAME: &str = "Calf Multi Chorus";
const BRAND: &str = "calf";

const PLUGIN_URI: &str = "http://calf.sourceforge.net/plugins/MultiChorus";
const PLUGIN_DIR: &str = "calf-bad";

#[cfg(target_os = "macos")]
const PLUGIN_BINARY: &str = "calf.dylib";
#[cfg(target_os = "linux")]
const PLUGIN_BINARY: &str = "calf.so";
#[cfg(target_os = "windows")]
const PLUGIN_BINARY: &str = "calf.dll";

// Port indices (from TTL)
const PORT_AUDIO_IN_IN_L: usize = 0;
const PORT_AUDIO_IN_IN_R: usize = 1;
const PORT_AUDIO_OUT_OUT_L: usize = 2;
const PORT_AUDIO_OUT_OUT_R: usize = 3;
const PORT_CTRL_MIN_DELAY: usize = 4;
const PORT_CTRL_MOD_DEPTH: usize = 5;
const PORT_CTRL_MOD_RATE: usize = 6;
const PORT_CTRL_STEREO: usize = 7;
const PORT_CTRL_VOICES: usize = 8;
const PORT_CTRL_VPHASE: usize = 9;
const PORT_CTRL_AMOUNT: usize = 10;
const PORT_CTRL_DRY: usize = 11;
const PORT_CTRL_FREQ: usize = 12;
const PORT_CTRL_FREQ2: usize = 13;
const PORT_CTRL_Q: usize = 14;
const PORT_CTRL_OVERLAP: usize = 15;

pub fn model_schema() -> ModelParameterSchema {
    ModelParameterSchema {
        effect_type: block_core::EFFECT_TYPE_MODULATION.into(),
        model: MODEL_ID.into(),
        display_name: DISPLAY_NAME.into(),
        audio_mode: ModelAudioMode::TrueStereo,
        parameters: vec![
            float_parameter("min_delay", "Min delay", None, Some(5.0), 0.1, 10.0, 0.01, ParameterUnit::Milliseconds),
            float_parameter("mod_depth", "Mod depth", None, Some(6.0), 0.1, 10.0, 0.01, ParameterUnit::Milliseconds),
            float_parameter("mod_rate", "Modulation rate", None, Some(0.5), 0.01, 20.0, 0.1, ParameterUnit::Hertz),
            float_parameter("stereo", "Stereo phase", None, Some(180.0), 0.0, 360.0, 1.0, ParameterUnit::None),
            float_parameter("voices", "Voices", None, Some(4.0), 1.0, 8.0, 0.01, ParameterUnit::None),
            float_parameter("vphase", "Inter-voice phase", None, Some(64.0), 0.0, 360.0, 1.0, ParameterUnit::None),
            float_parameter("amount", "Amount", None, Some(1.0), 0.0, 4.0, 0.01, ParameterUnit::None),
            float_parameter("dry", "Dry Amount", None, Some(0.5), 0.0, 4.0, 0.01, ParameterUnit::None),
            float_parameter("freq", "Center Frq 1", None, Some(100.0), 10.0, 20000.0, 10.0, ParameterUnit::Hertz),
            float_parameter("freq2", "Center Frq 2", None, Some(5000.0), 10.0, 20000.0, 10.0, ParameterUnit::Hertz),
            float_parameter("q", "Q", None, Some(0.125), 0.125, 8.0, 0.01, ParameterUnit::None),
            float_parameter("overlap", "Overlap", None, Some(0.75), 0.0, 1.0, 0.01, ParameterUnit::None),
        ],
    }
}

fn build(params: &ParameterSet, sample_rate: f32, layout: AudioChannelLayout) -> Result<BlockProcessor> {
    let min_delay = required_f32(params, "min_delay").map_err(anyhow::Error::msg)?;
    let mod_depth = required_f32(params, "mod_depth").map_err(anyhow::Error::msg)?;
    let mod_rate = required_f32(params, "mod_rate").map_err(anyhow::Error::msg)?;
    let stereo = required_f32(params, "stereo").map_err(anyhow::Error::msg)?;
    let voices = required_f32(params, "voices").map_err(anyhow::Error::msg)?;
    let vphase = required_f32(params, "vphase").map_err(anyhow::Error::msg)?;
    let amount = required_f32(params, "amount").map_err(anyhow::Error::msg)?;
    let dry = required_f32(params, "dry").map_err(anyhow::Error::msg)?;
    let freq = required_f32(params, "freq").map_err(anyhow::Error::msg)?;
    let freq2 = required_f32(params, "freq2").map_err(anyhow::Error::msg)?;
    let q = required_f32(params, "q").map_err(anyhow::Error::msg)?;
    let overlap = required_f32(params, "overlap").map_err(anyhow::Error::msg)?;
    let lib_path = lv2::resolve_lv2_lib(PLUGIN_BINARY)?;
    let bundle_path = lv2::resolve_lv2_bundle(PLUGIN_DIR)?;

    let control_ports: &[(usize, f32)] = &[
        (PORT_CTRL_MIN_DELAY, min_delay),
        (PORT_CTRL_MOD_DEPTH, mod_depth),
        (PORT_CTRL_MOD_RATE, mod_rate),
        (PORT_CTRL_STEREO, stereo),
        (PORT_CTRL_VOICES, voices),
        (PORT_CTRL_VPHASE, vphase),
        (PORT_CTRL_AMOUNT, amount),
        (PORT_CTRL_DRY, dry),
        (PORT_CTRL_FREQ, freq),
        (PORT_CTRL_FREQ2, freq2),
        (PORT_CTRL_Q, q),
        (PORT_CTRL_OVERLAP, overlap),
    ];

    let processor = lv2::build_stereo_lv2_processor(&lib_path, PLUGIN_URI, sample_rate as f64, &bundle_path, &[PORT_AUDIO_IN_IN_L, PORT_AUDIO_IN_IN_R], &[PORT_AUDIO_OUT_OUT_L, PORT_AUDIO_OUT_OUT_R], control_ports)?;
    let _ = layout;
    Ok(BlockProcessor::Stereo(Box::new(processor)))
}

fn schema() -> Result<ModelParameterSchema> { Ok(model_schema()) }

pub const MODEL_DEFINITION: ModModelDefinition = ModModelDefinition {
    id: MODEL_ID, display_name: DISPLAY_NAME, brand: BRAND,
    backend_kind: ModBackendKind::Lv2, schema, build,
    supported_instruments: block_core::ALL_INSTRUMENTS, knob_layout: &[],
};
