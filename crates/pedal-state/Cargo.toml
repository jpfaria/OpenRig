[package]
name = "pedal-state"
version.workspace = true
edition.workspace = true

[dependencies]
serde.workspace = true
pedal-domain = { path = "../pedal-domain" }
pedal-preset = { path = "../pedal-preset" }
pedal-setup = { path = "../pedal-setup" }
