// AUTO-GENERATED by tools/lv2_catalog_import — do not edit by hand.
// Re-run the tool to regenerate. Manual customisation goes in overrides.yaml.

#![allow(dead_code, unused_imports, unused_variables, clippy::approx_constant, clippy::excessive_precision)]

use crate::registry::ReverbModelDefinition;
use crate::ReverbBackendKind;
use anyhow::Result;
use block_core::param::{
    float_parameter, required_f32, ModelParameterSchema, ParameterSet, ParameterUnit,
};
use block_core::{AudioChannelLayout, BlockProcessor, ModelAudioMode};

pub const MODEL_ID: &str = "lv2_dragonfly_room_reverb";
pub const DISPLAY_NAME: &str = "Dragonfly Room Reverb";
const BRAND: &str = "dragonfly";

const PLUGIN_URI: &str = "urn:dragonfly:room";
const PLUGIN_DIR: &str = "DragonflyRoomReverb";

#[cfg(target_os = "macos")]
const PLUGIN_BINARY: &str = "DragonflyRoomReverb_dsp.dylib.dylib";
#[cfg(target_os = "linux")]
const PLUGIN_BINARY: &str = "DragonflyRoomReverb_dsp.dylib.so";
#[cfg(target_os = "windows")]
const PLUGIN_BINARY: &str = "DragonflyRoomReverb_dsp.dylib.dll";

// Port indices (from TTL)
const PORT_AUDIO_IN_LV2_AUDIO_IN_1: usize = 0;
const PORT_AUDIO_IN_LV2_AUDIO_IN_2: usize = 1;
const PORT_AUDIO_OUT_LV2_AUDIO_OUT_1: usize = 2;
const PORT_AUDIO_OUT_LV2_AUDIO_OUT_2: usize = 3;
const PORT_ATOM_IN_LV2_EVENTS_IN: usize = 4;
const PORT_ATOM_OUT_LV2_EVENTS_OUT: usize = 5;
const PORT_CTRL_DRY_LEVEL: usize = 6;
const PORT_CTRL_EARLY_LEVEL: usize = 7;
const PORT_CTRL_EARLY_SEND: usize = 8;
const PORT_CTRL_LATE_LEVEL: usize = 9;
const PORT_CTRL_SIZE: usize = 10;
const PORT_CTRL_WIDTH: usize = 11;
const PORT_CTRL_PREDELAY: usize = 12;
const PORT_CTRL_DECAY: usize = 13;
const PORT_CTRL_DIFFUSE: usize = 14;
const PORT_CTRL_SPIN: usize = 15;
const PORT_CTRL_WANDER: usize = 16;
const PORT_CTRL_IN_HIGH_CUT: usize = 17;
const PORT_CTRL_EARLY_DAMP: usize = 18;
const PORT_CTRL_LATE_DAMP: usize = 19;
const PORT_CTRL_LOW_BOOST: usize = 20;
const PORT_CTRL_BOOST_FREQ: usize = 21;
const PORT_CTRL_IN_LOW_CUT: usize = 22;

pub fn model_schema() -> ModelParameterSchema {
    ModelParameterSchema {
        effect_type: block_core::EFFECT_TYPE_REVERB.into(),
        model: MODEL_ID.into(),
        display_name: DISPLAY_NAME.into(),
        audio_mode: ModelAudioMode::TrueStereo,
        parameters: vec![
            float_parameter("dry_level", "Dry Level", None, Some(80.0), 0.0, 100.0, 1.0, ParameterUnit::Percent),
            float_parameter("early_level", "Early Level", None, Some(10.0), 0.0, 100.0, 1.0, ParameterUnit::Percent),
            float_parameter("early_send", "Early Send", None, Some(20.0), 0.0, 100.0, 1.0, ParameterUnit::Percent),
            float_parameter("late_level", "Late Level", None, Some(20.0), 0.0, 100.0, 1.0, ParameterUnit::Percent),
            float_parameter("size", "Size", None, Some(12.0), 8.0, 32.0, 0.1, ParameterUnit::None),
            float_parameter("width", "Width", None, Some(100.0), 50.0, 150.0, 1.0, ParameterUnit::Percent),
            float_parameter("predelay", "Predelay", None, Some(8.0), 0.0, 100.0, 1.0, ParameterUnit::Milliseconds),
            float_parameter("decay", "Decay", None, Some(0.4), 0.1, 10.0, 0.01, ParameterUnit::Milliseconds),
            float_parameter("diffuse", "Diffuse", None, Some(70.0), 0.0, 100.0, 1.0, ParameterUnit::Percent),
            float_parameter("spin", "Spin", None, Some(0.8), 0.0, 5.0, 0.01, ParameterUnit::Hertz),
            float_parameter("wander", "Wander", None, Some(40.0), 0.0, 100.0, 1.0, ParameterUnit::Percent),
            float_parameter("in_high_cut", "High Cut", None, Some(16000.0), 1000.0, 16000.0, 10.0, ParameterUnit::Hertz),
            float_parameter("early_damp", "Early Damp", None, Some(10000.0), 1000.0, 16000.0, 10.0, ParameterUnit::Hertz),
            float_parameter("late_damp", "Late Damp", None, Some(9400.0), 1000.0, 16000.0, 10.0, ParameterUnit::Hertz),
            float_parameter("low_boost", "Low Boost", None, Some(50.0), 0.0, 100.0, 1.0, ParameterUnit::Percent),
            float_parameter("boost_freq", "Boost Freq", None, Some(600.0), 50.0, 1050.0, 10.0, ParameterUnit::Hertz),
            float_parameter("in_low_cut", "Low Cut", None, Some(4.0), 0.0, 200.0, 1.0, ParameterUnit::Hertz),
        ],
    }
}

fn build(params: &ParameterSet, sample_rate: f32, layout: AudioChannelLayout) -> Result<BlockProcessor> {
    let dry_level = required_f32(params, "dry_level").map_err(anyhow::Error::msg)?;
    let early_level = required_f32(params, "early_level").map_err(anyhow::Error::msg)?;
    let early_send = required_f32(params, "early_send").map_err(anyhow::Error::msg)?;
    let late_level = required_f32(params, "late_level").map_err(anyhow::Error::msg)?;
    let size = required_f32(params, "size").map_err(anyhow::Error::msg)?;
    let width = required_f32(params, "width").map_err(anyhow::Error::msg)?;
    let predelay = required_f32(params, "predelay").map_err(anyhow::Error::msg)?;
    let decay = required_f32(params, "decay").map_err(anyhow::Error::msg)?;
    let diffuse = required_f32(params, "diffuse").map_err(anyhow::Error::msg)?;
    let spin = required_f32(params, "spin").map_err(anyhow::Error::msg)?;
    let wander = required_f32(params, "wander").map_err(anyhow::Error::msg)?;
    let in_high_cut = required_f32(params, "in_high_cut").map_err(anyhow::Error::msg)?;
    let early_damp = required_f32(params, "early_damp").map_err(anyhow::Error::msg)?;
    let late_damp = required_f32(params, "late_damp").map_err(anyhow::Error::msg)?;
    let low_boost = required_f32(params, "low_boost").map_err(anyhow::Error::msg)?;
    let boost_freq = required_f32(params, "boost_freq").map_err(anyhow::Error::msg)?;
    let in_low_cut = required_f32(params, "in_low_cut").map_err(anyhow::Error::msg)?;
    let lib_path = lv2::resolve_lv2_lib(PLUGIN_BINARY)?;
    let bundle_path = lv2::resolve_lv2_bundle(PLUGIN_DIR)?;

    let control_ports: &[(usize, f32)] = &[
        (PORT_CTRL_DRY_LEVEL, dry_level),
        (PORT_CTRL_EARLY_LEVEL, early_level),
        (PORT_CTRL_EARLY_SEND, early_send),
        (PORT_CTRL_LATE_LEVEL, late_level),
        (PORT_CTRL_SIZE, size),
        (PORT_CTRL_WIDTH, width),
        (PORT_CTRL_PREDELAY, predelay),
        (PORT_CTRL_DECAY, decay),
        (PORT_CTRL_DIFFUSE, diffuse),
        (PORT_CTRL_SPIN, spin),
        (PORT_CTRL_WANDER, wander),
        (PORT_CTRL_IN_HIGH_CUT, in_high_cut),
        (PORT_CTRL_EARLY_DAMP, early_damp),
        (PORT_CTRL_LATE_DAMP, late_damp),
        (PORT_CTRL_LOW_BOOST, low_boost),
        (PORT_CTRL_BOOST_FREQ, boost_freq),
        (PORT_CTRL_IN_LOW_CUT, in_low_cut),
    ];

    let processor = lv2::build_stereo_lv2_processor(&lib_path, PLUGIN_URI, sample_rate as f64, &bundle_path, &[PORT_AUDIO_IN_LV2_AUDIO_IN_1, PORT_AUDIO_IN_LV2_AUDIO_IN_2], &[PORT_AUDIO_OUT_LV2_AUDIO_OUT_1, PORT_AUDIO_OUT_LV2_AUDIO_OUT_2], control_ports)?;
    let _ = layout;
    Ok(BlockProcessor::Stereo(Box::new(processor)))
}

fn schema() -> Result<ModelParameterSchema> { Ok(model_schema()) }

pub const MODEL_DEFINITION: ReverbModelDefinition = ReverbModelDefinition {
    id: MODEL_ID, display_name: DISPLAY_NAME, brand: BRAND,
    backend_kind: ReverbBackendKind::Lv2, schema, build,
    supported_instruments: block_core::ALL_INSTRUMENTS, knob_layout: &[],
};
