// Tracks catalog screen (#553).
//
// Layout: search bar + import button on top, list of cards (Moises-
// style) below. Empty state when the catalog is empty. Routing into
// this page and the Track detail view land in follow-up commits.

import { ScrollView, LineEdit } from "std-widgets.slint";

export struct TrackRowData {
    id: string,
    title: string,
    artist: string,
    duration: string,
    stem-count: int,
    cover-art: image,
}

export component TracksPage inherits Rectangle {
    in property <[TrackRowData]> tracks;
    in-out property <string> search-text;

    callback import-clicked();
    callback track-clicked(string);
    callback search-changed(string);
    callback back-clicked();

    background: #15171a;

    VerticalLayout {
        padding: 24px;
        spacing: 16px;

        // Header
        HorizontalLayout {
            spacing: 12px;
            alignment: stretch;

            Rectangle {
                width: 64px;
                height: 32px;
                background: #2f3339;
                border-width: 1px;
                border-color: #4c515b;
                border-radius: 4px;
                y: 8px;

                Text {
                    text: @tr("tracks-back");
                    color: #f4f7fb;
                    font-size: 12px;
                    horizontal-alignment: center;
                    vertical-alignment: center;
                }
                TouchArea { clicked => { root.back-clicked(); } }
            }

            Text {
                text: @tr("tracks-title");
                color: #f4f7fb;
                font-size: 22px;
                font-weight: 700;
                vertical-alignment: center;
                horizontal-stretch: 1;
            }

            Rectangle {
                width: 220px;
                height: 36px;
                background: #2f3339;
                border-width: 1px;
                border-color: #4c515b;
                border-radius: 4px;

                LineEdit {
                    x: 8px;
                    y: 0px;
                    width: parent.width - 16px;
                    height: parent.height;
                    placeholder-text: @tr("tracks-search-placeholder");
                    text <=> root.search-text;
                    edited(t) => { root.search-changed(t); }
                }
            }

            Rectangle {
                width: 180px;
                height: 36px;
                background: #188cff;
                border-width: 1px;
                border-color: #45a7ff;
                border-radius: 4px;

                Text {
                    text: @tr("tracks-import");
                    color: #f4f7fb;
                    font-size: 13px;
                    font-weight: 600;
                    horizontal-alignment: center;
                    vertical-alignment: center;
                }

                TouchArea {
                    clicked => { root.import-clicked(); }
                }
            }
        }

        // Empty state OR list
        if root.tracks.length == 0 : Rectangle {
            background: transparent;
            vertical-stretch: 1;

            VerticalLayout {
                spacing: 14px;
                alignment: center;
                width: parent.width;
                height: parent.height;

                Rectangle {
                    width: 96px;
                    height: 96px;
                    background: @linear-gradient(180deg, #2a2d33 0%, #16181c 100%);
                    border-radius: 16px;
                    border-width: 1px;
                    border-color: #3d4148;
                    horizontal-stretch: 0;

                    Image {
                        x: 22px;
                        y: 22px;
                        width: 52px;
                        height: 52px;
                        source: @image-url("../assets/tracks.svg");
                        image-fit: contain;
                        colorize: #6e7682;
                    }
                }

                Text {
                    text: @tr("tracks-empty");
                    color: #b6bcc4;
                    font-size: 16px;
                    font-weight: 600;
                    horizontal-alignment: center;
                }
                Text {
                    text: @tr("tracks-empty-hint");
                    color: #6e7682;
                    font-size: 12px;
                    horizontal-alignment: center;
                }
            }
        }

        if root.tracks.length > 0 : ScrollView {
            vertical-stretch: 1;

            VerticalLayout {
                spacing: 8px;

                for track in root.tracks : Rectangle {
                    height: 72px;
                    background: @linear-gradient(180deg, #1d2024 0%, #16181c 100%);
                    border-width: 1px;
                    border-color: #2f3339;
                    border-radius: 8px;
                    drop-shadow-color: #00000060;
                    drop-shadow-offset-y: 1px;
                    drop-shadow-blur: 3px;

                    HorizontalLayout {
                        padding: 10px;
                        spacing: 14px;
                        alignment: stretch;

                        // Cover art (or stylized placeholder when no embedded image).
                        Rectangle {
                            width: 52px;
                            height: 52px;
                            background: @linear-gradient(180deg, #2a2d33 0%, #16181c 100%);
                            border-width: 1px;
                            border-color: #3d4148;
                            border-radius: 6px;
                            clip: true;

                            Image {
                                width: parent.width;
                                height: parent.height;
                                source: track.cover-art;
                                image-fit: cover;
                            }
                        }

                        VerticalLayout {
                            horizontal-stretch: 1;
                            spacing: 2px;
                            alignment: center;
                            Text {
                                text: track.title;
                                color: #f4f7fb;
                                font-size: 15px;
                                font-weight: 700;
                            }
                            HorizontalLayout {
                                spacing: 8px;
                                Text {
                                    text: track.artist;
                                    color: #b6bcc4;
                                    font-size: 12px;
                                }
                                Text {
                                    text: track.stem-count + " stems";
                                    color: #8a929e;
                                    font-size: 11px;
                                    font-weight: 600;
                                }
                            }
                        }
                        Text {
                            text: track.duration;
                            color: #8a929e;
                            font-size: 14px;
                            font-weight: 600;
                            vertical-alignment: center;
                            width: 60px;
                            horizontal-alignment: right;
                        }
                    }

                    TouchArea {
                        clicked => { root.track-clicked(track.id); }
                    }
                }
            }
        }
    }
}
