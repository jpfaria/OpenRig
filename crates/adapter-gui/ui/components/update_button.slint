// Responsibility: renders the button that installs a newer OpenRig release.
import { Theme } from "../theme.slint";

export component UpdateButton inherits Rectangle {
    in property <string> latest-version;
    callback clicked();

    width: row.preferred-width;
    height: 26px;
    border-radius: 4px;
    background: touch.pressed ? #1273d6 : touch.has-hover ? #2a98ff : #188cff;
    border-width: 1px;
    border-color: #45a7ff;
    accessible-role: button;
    accessible-label: @tr("btn-update-to {}", root.latest-version);
    accessible-action-default => { root.clicked(); }

    row := HorizontalLayout {
        padding-left: 8px;
        padding-right: 10px;
        spacing: 6px;
        alignment: center;

        Image {
            width: 16px;
            height: 16px;
            y: (parent.height - self.height) / 2;
            source: @image-url("../assets/arrow-down.svg");
            colorize: #f4f7fb;
        }

        Text {
            text: @tr("btn-update-to {}", root.latest-version);
            color: #f4f7fb;
            font-size: Theme.min-font;
            font-weight: 600;
            vertical-alignment: center;
        }
    }

    touch := TouchArea {
        mouse-cursor: pointer;
        clicked => { root.clicked(); }
    }
}
