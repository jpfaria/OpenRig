// Runtime helper functions are defined in the crate root (lib.rs) as pub(crate).
// They are accessible to submodules via `crate::function_name`.
// This module exists as a placeholder for future extraction of these helpers.
