[package]
name = "application"
version.workspace = true
edition.workspace = true

[dependencies]
anyhow.workspace = true
ports = { path = "../ports" }
preset = { path = "../preset" }
setup = { path = "../setup" }
state = { path = "../state" }
