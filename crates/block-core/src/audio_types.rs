//! Responsibility: keeps the historical `audio_types` path pointing at the audio types.

pub use crate::channel_layout::{AudioChannelLayout, ModelAudioMode};
pub use crate::stream_handle::{new_stream_handle, StreamEntry, StreamHandle};
