// AUTO-GENERATED by tools/lv2_catalog_import — do not edit by hand.
// Re-run the tool to regenerate. Manual customisation goes in overrides.yaml.

#![allow(dead_code, unused_imports, unused_variables, clippy::approx_constant, clippy::excessive_precision)]

use crate::registry::DelayModelDefinition;
use crate::DelayBackendKind;
use anyhow::Result;
use block_core::param::{
    bool_parameter, float_parameter, required_bool, required_f32,
    ModelParameterSchema, ParameterSet, ParameterUnit,
};
use block_core::{AudioChannelLayout, BlockProcessor, ModelAudioMode};

pub const MODEL_ID: &str = "lv2_rkr_echotron";
pub const DISPLAY_NAME: &str = "rkr Echotron";
const BRAND: &str = "rkr";

const PLUGIN_URI: &str = "http://rakarrack.sourceforge.net/effects.html#Echotron";
const PLUGIN_DIR: &str = "rkr";

#[cfg(target_os = "macos")]
const PLUGIN_BINARY: &str = "plugin.dylib";
#[cfg(target_os = "linux")]
const PLUGIN_BINARY: &str = "plugin.so";
#[cfg(target_os = "windows")]
const PLUGIN_BINARY: &str = "plugin.dll";

// Port indices (from TTL)
const PORT_AUDIO_IN_INPUT_L: usize = 0;
const PORT_AUDIO_IN_INPUT_R: usize = 1;
const PORT_AUDIO_OUT_OUTPUT_L: usize = 2;
const PORT_AUDIO_OUT_OUTPUT_R: usize = 3;
const PORT_ATOM_IN_CONTROL: usize = 4;
const PORT_ATOM_OUT_NOTIFY: usize = 5;
const PORT_CTRL_BYPASS: usize = 6;
const PORT_CTRL_WETDRY: usize = 7;
const PORT_CTRL_DEPTH: usize = 8;
const PORT_CTRL_WIDTH: usize = 9;
const PORT_CTRL_LENGTH: usize = 10;
const PORT_CTRL_TEMPO: usize = 11;
const PORT_CTRL_DAMP: usize = 12;
const PORT_CTRL_LRC: usize = 13;
const PORT_CTRL_STDF: usize = 14;
const PORT_CTRL_FB: usize = 15;
const PORT_CTRL_PAN: usize = 16;
const PORT_CTRL_MD: usize = 17;
const PORT_CTRL_MF: usize = 18;
const PORT_CTRL_LFOTYPE: usize = 19;
const PORT_CTRL_AF: usize = 20;

pub fn model_schema() -> ModelParameterSchema {
    ModelParameterSchema {
        effect_type: block_core::EFFECT_TYPE_DELAY.into(),
        model: MODEL_ID.into(),
        display_name: DISPLAY_NAME.into(),
        audio_mode: ModelAudioMode::TrueStereo,
        parameters: vec![
            bool_parameter("bypass", "Bypass", None, Some(false)),
            float_parameter("wetdry", "Wet/Dry", None, Some(64.0), 0.0, 127.0, 1.0, ParameterUnit::None),
            float_parameter("depth", "Filter Depth", None, Some(0.0), -64.0, 64.0, 1.0, ParameterUnit::None),
            float_parameter("width", "LFO Width", None, Some(41.0), 0.0, 127.0, 1.0, ParameterUnit::None),
            float_parameter("length", "Number of Taps", None, Some(127.0), 1.0, 127.0, 1.0, ParameterUnit::None),
            float_parameter("tempo", "TEMPO", None, Some(64.0), 1.0, 600.0, 1.0, ParameterUnit::None),
            float_parameter("damp", "Dampening", None, Some(99.0), 0.0, 127.0, 1.0, ParameterUnit::None),
            float_parameter("lrc", "Left/Right Crossover", None, Some(20.0), -64.0, 64.0, 1.0, ParameterUnit::None),
            float_parameter("stdf", "LFO L/R Delay", None, Some(0.0), -64.0, 64.0, 1.0, ParameterUnit::None),
            float_parameter("fb", "Feedback", None, Some(0.0), -64.0, 64.0, 1.0, ParameterUnit::None),
            float_parameter("pan", "Pan", None, Some(0.0), -64.0, 63.0, 1.0, ParameterUnit::None),
            bool_parameter("md", "Modulate Delays", None, Some(false)),
            bool_parameter("mf", "Modulate Filters", None, Some(false)),
            float_parameter("lfotype", "LFO Type", None, Some(0.0), 0.0, 11.0, 0.1, ParameterUnit::None),
            bool_parameter("af", "Activate Filters", None, Some(false)),
        ],
    }
}

fn build(params: &ParameterSet, sample_rate: f32, layout: AudioChannelLayout) -> Result<BlockProcessor> {
    let bypass = required_bool(params, "bypass").map_err(anyhow::Error::msg)?;
    let wetdry = required_f32(params, "wetdry").map_err(anyhow::Error::msg)?;
    let depth = required_f32(params, "depth").map_err(anyhow::Error::msg)?;
    let width = required_f32(params, "width").map_err(anyhow::Error::msg)?;
    let length = required_f32(params, "length").map_err(anyhow::Error::msg)?;
    let tempo = required_f32(params, "tempo").map_err(anyhow::Error::msg)?;
    let damp = required_f32(params, "damp").map_err(anyhow::Error::msg)?;
    let lrc = required_f32(params, "lrc").map_err(anyhow::Error::msg)?;
    let stdf = required_f32(params, "stdf").map_err(anyhow::Error::msg)?;
    let fb = required_f32(params, "fb").map_err(anyhow::Error::msg)?;
    let pan = required_f32(params, "pan").map_err(anyhow::Error::msg)?;
    let md = required_bool(params, "md").map_err(anyhow::Error::msg)?;
    let mf = required_bool(params, "mf").map_err(anyhow::Error::msg)?;
    let lfotype = required_f32(params, "lfotype").map_err(anyhow::Error::msg)?;
    let af = required_bool(params, "af").map_err(anyhow::Error::msg)?;
    let lib_path = lv2::resolve_lv2_lib(PLUGIN_BINARY)?;
    let bundle_path = lv2::resolve_lv2_bundle(PLUGIN_DIR)?;

    let control_ports: &[(usize, f32)] = &[
        (PORT_CTRL_BYPASS, if bypass { 1.0 } else { 0.0 }),
        (PORT_CTRL_WETDRY, wetdry),
        (PORT_CTRL_DEPTH, depth),
        (PORT_CTRL_WIDTH, width),
        (PORT_CTRL_LENGTH, length),
        (PORT_CTRL_TEMPO, tempo),
        (PORT_CTRL_DAMP, damp),
        (PORT_CTRL_LRC, lrc),
        (PORT_CTRL_STDF, stdf),
        (PORT_CTRL_FB, fb),
        (PORT_CTRL_PAN, pan),
        (PORT_CTRL_MD, if md { 1.0 } else { 0.0 }),
        (PORT_CTRL_MF, if mf { 1.0 } else { 0.0 }),
        (PORT_CTRL_LFOTYPE, lfotype),
        (PORT_CTRL_AF, if af { 1.0 } else { 0.0 }),
    ];

    let processor = lv2::build_stereo_lv2_processor(&lib_path, PLUGIN_URI, sample_rate as f64, &bundle_path, &[PORT_AUDIO_IN_INPUT_L, PORT_AUDIO_IN_INPUT_R], &[PORT_AUDIO_OUT_OUTPUT_L, PORT_AUDIO_OUT_OUTPUT_R], control_ports)?;
    let _ = layout;
    Ok(BlockProcessor::Stereo(Box::new(processor)))
}

fn schema() -> Result<ModelParameterSchema> { Ok(model_schema()) }

pub const MODEL_DEFINITION: DelayModelDefinition = DelayModelDefinition {
    id: MODEL_ID, display_name: DISPLAY_NAME, brand: BRAND,
    backend_kind: DelayBackendKind::Lv2, schema, build,
    supported_instruments: block_core::ALL_INSTRUMENTS, knob_layout: &[],
};
