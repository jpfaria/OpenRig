[package]
name = "nam"
version.workspace = true
edition.workspace = true
[dependencies]
anyhow.workspace = true
stage-core = { path = "../stage-core" }
domain = { path = "../domain" }
[build-dependencies]
cmake = "0.1"
