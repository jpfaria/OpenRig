[package]
name = "nam"
version.workspace = true
edition.workspace = true
[dependencies]
anyhow.workspace = true
block-core = { path = "../block-core" }
plugin-loader = { path = "../plugin-loader" }
domain = { path = "../domain" }
log.workspace = true
[build-dependencies]
cmake = "0.1"
# #974: the vendored NeuralAmpModelerCore archive (Git LFS) is extracted by the
# build — see build_vendor.rs.
flate2 = "1"
tar = "0.4"

[dev-dependencies]
flate2 = "1"
tar = "0.4"
tempfile = "3"
