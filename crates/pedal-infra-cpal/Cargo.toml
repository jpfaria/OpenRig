[package]
name = "pedal-infra-cpal"
version.workspace = true
edition.workspace = true
[dependencies]
anyhow.workspace = true
cpal.workspace = true
pedal-domain = { path = "../pedal-domain" }
pedal-engine = { path = "../pedal-engine" }
pedal-setup = { path = "../pedal-setup" }
