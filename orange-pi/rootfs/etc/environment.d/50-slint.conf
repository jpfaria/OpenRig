# Slint is running as a Wayland client under Weston (see weston.service).
# The `linuxkms` backend would try to grab DRM directly, which conflicts
# with Weston's DRM master. `winit` autodetects Wayland when WAYLAND_DISPLAY
# is set by openrig.service.
SLINT_BACKEND=winit
SLINT_RENDERER=software
