# OpenRig — PREEMPT_RT kernel config fragment for Orange Pi 5B
# Applied on top of Armbian default config during build.
# Enables full real-time preemption for low-latency JACK audio.
CONFIG_PREEMPT_RT=y
